import { pageMetadata } from '@/lib/seo';
export const metadata = pageMetadata('Parent Resources', 'Access calendars, policies, uniform guidance, downloads and helpful information for Creative All Stars Academy families.', '/parents-corner');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
