import { pageMetadata } from '@/lib/seo';
export const metadata = pageMetadata('Competency-Based Education and Learning', 'Explore learner-centred competency-based education and academic pathways at Creative All Stars Academy in Nakuru.', '/academics');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
