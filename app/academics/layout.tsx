import { pageMetadata } from '@/lib/seo';
export const metadata = pageMetadata('CBC Academics and Learning', 'Explore the practical, learner-centred CBC curriculum and academic pathways at Creative All Stars Academy in Nakuru.', '/academics');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
