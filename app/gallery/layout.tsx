import { pageMetadata } from '@/lib/seo';
export const metadata = pageMetadata('School Gallery and Campus Life', 'See learning, sport, events and everyday school life at Creative All Stars Academy in Nakuru.', '/gallery');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
