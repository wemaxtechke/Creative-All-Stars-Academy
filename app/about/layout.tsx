import { pageMetadata } from '@/lib/seo';
export const metadata = pageMetadata('About Our School', 'Learn about Creative All Stars Academy’s history, inclusive vision, holistic mission, values and teaching team in Nakuru.', '/about');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
