import { pageMetadata } from '@/lib/seo';
export const metadata = pageMetadata('School News and Insights', 'Read school news, events, competency-based learning insights and parent resources from Creative All Stars Academy Nakuru.', '/blog');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
