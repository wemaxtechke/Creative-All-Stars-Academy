import { pageMetadata } from '@/lib/seo';
export const metadata = pageMetadata('Admissions and School Enquiries', 'Start an admission enquiry, learn about fees and discover how to join Creative All Stars Academy in Nakuru.', '/admissions');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
