import { pageMetadata } from '@/lib/seo';
export const metadata = pageMetadata('Classes and Grade Levels', 'Discover the classes, age groups and learner experiences available at Creative All Stars Academy Nakuru.', '/classes');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
